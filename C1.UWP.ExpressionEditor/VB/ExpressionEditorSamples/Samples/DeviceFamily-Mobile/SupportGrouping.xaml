<Page
    x:Class="ExpressionEditorSamples.SupportGrouping"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c1="using:C1.Xaml.FlexGrid"
    xmlns:local="using:ExpressionEditorSamples"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Frame x:Name="frame">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Button x:Name="group" Click="group_Click"  Content="Add expression to C1FlexGrid groups." HorizontalAlignment="Center"/>
                <c1:C1FlexGrid x:Name="flexGrid" Grid.Row="1"/>
            </Grid>
        </Frame>
    </Grid>
</Page>
